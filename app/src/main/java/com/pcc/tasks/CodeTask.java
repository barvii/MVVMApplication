package com.pcc.tasks;

public class CodeTask {

    /**
     * You are given a two-digit integer n. Return the sum of its digits.
     *
     * Example:
     * For n = 29, it's 2 + 9, so the output should be
     * task1(n) = 11
     */
    public static int task1(int n) {
        return n;
    }


    /**
     * Given an integer n, return the largest number that contains exactly n digits.
     *
     * Example:
     * For n = 2, the output should be
     * solution(n) = 99.
     */
    public static int task2(int n) {
        return n;
    }

    /**
     * You are given an array of two non negative integers, each less than 256.
     *
     * Your task is to pack these integers into one number M in the following way:
     *
     * The first element of the array occupies the first 8 bits of M;
     * The second element occupies next 8 bits.
     * Return the obtained integer M.
     *
     * Example:
     *
     * For a = [24, 85], binary representation looks like [00011000, 01010101].
     * After packing these ints into one number we get 01010101 00011000
     * (spaces are placed for convenience), which equals to 21784.
     */
    public static int task3(int[] a) {
        return 0;
    }
}
